import login from "./login/login-en.json"
import register from "./register/register-en.json"
import shared from "./shared/shared-en.json"

export default {
  login: login,
  register: register,
  ...shared,
}
